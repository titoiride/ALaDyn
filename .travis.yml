language: cpp

matrix:
  include:
    - os: osx
      env:
        - MATRIX_EVAL="brew update && brew cask uninstall oclint && brew install gcc@5 open-mpi fftw && brew upgrade cmake && CC=gcc-5 && CXX=g++-5 && FC=/usr/local/bin/gfortran-5"

### broken, still don't know why
#    - os: osx
#      env:
#        - MATRIX_EVAL="brew update && brew cask uninstall oclint && brew install gcc@6 open-mpi fftw && brew upgrade cmake && CC=gcc-6 && CXX=g++-6 && FC=/usr/local/bin/gfortran-6"
#
#    - os: osx
#      env:
#        - MATRIX_EVAL="brew update && brew cask uninstall oclint && brew install gcc@7 open-mpi fftw && brew upgrade cmake && CC=gcc-7 && CXX=g++-7 && FC=/usr/local/bin/gfortran-7"
#
#    - os: osx
#      env:
#        - MATRIX_EVAL="brew update && brew cask uninstall oclint && brew install gcc open-mpi fftw && brew upgrade cmake && CC=gcc-8 && CXX=g++-8 && FC=/usr/local/bin/gfortran-8"

    - os: osx
      env:
        - MATRIX_EVAL="brew update && brew cask uninstall oclint && brew install gcc open-mpi fftw && brew upgrade cmake && CC=gcc && CXX=g++ && FC=/usr/local/bin/gfortran-8"

    - os: osx
      env:
        - MATRIX_EVAL="brew update && brew cask uninstall oclint && brew install gcc open-mpi fftw && brew upgrade cmake && CC=clang && CXX=clang++ && FC=/usr/local/bin/gfortran-8"

    - os: linux
      addons:
        apt:
          packages:
            - clang-3.6
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL="CC=clang-3.6 && CXX=clang++-3.6"

    - os: linux
      addons:
        apt:
          sources:
            - llvm-toolchain-precise-3.8
          packages:
            - clang-3.8
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL="CC=clang-3.8 && CXX=clang++-3.8"

    - os: linux
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-3.9
          packages:
            - clang-3.9
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL="CC=clang-3.9 && CXX=clang++-3.9"

    - os: linux
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-4.0
          packages:
            - clang-4.0
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL="CC=clang-4.0 && CXX=clang++-4.0"

    - os: linux
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-5.0
          packages:
            - clang-5.0
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL="CC=clang-5.0 && CXX=clang++-5.0"

    - os: linux
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-6.0
            - ubuntu-toolchain-r-test
          packages:
            - clang-6.0
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL="CC=clang-6.0 && CXX=clang++-6.0"

    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-4.9
            - gfortran-4.9
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
         - MATRIX_EVAL="CC=gcc-4.9 && CXX=g++-4.9 && FC=gfortran-4.9"

    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-5
            - gfortran-5
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
         - MATRIX_EVAL="CC=gcc-5 && CXX=g++-5 && FC=gfortran-5"

    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-6
            - gfortran-6
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL="CC=gcc-6 && CXX=g++-6 && FC=gfortran-6"

    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7
            - gfortran-7
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7 && FC=gfortran-7"

    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-8
            - gfortran-8
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL="CC=gcc-8 && CXX=g++-8 && FC=gfortran-8"

    - os: linux
      addons:
        apt:
          packages:
            - gfortran
            - libopenmpi-dev
            - libboost-all-dev
            - libfftw3-dev
      env:
        - MATRIX_EVAL=""

before_install:
  - eval "${MATRIX_EVAL}"

before_script:
  - mkdir build
  - cd build
  - cmake .. -DCMAKE_BUILD_TYPE="Debug"

script:
  - cmake --build . --target install

